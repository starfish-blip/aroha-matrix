class FrequencyMixer { constructor() { this.baseHz = 272.0; this.chromaticHouses = 12; this.semitoneRatio = Math.pow(2, 1/12); } generateDial() { console.log(`[AROHA-ACOUSTIC] Initializing 12-House Chromatic Dial from Base: ${this.baseHz}Hz`); const dial = []; for (let i = 0; i < this.chromaticHouses; i++) { const freq = parseFloat((this.baseHz * Math.pow(this.semitoneRatio, i)).toFixed(2)); dial.push({ house: i + 1, frequencyHz: freq }); console.log(`[HOUSE ${i + 1}] Frequency: ${freq}Hz`); } return dial; } } new FrequencyMixer().generateDial();